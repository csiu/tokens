library(shiny)
shinyServer(function(input, output) {
  
  # reactive expression
  # x <- reactive({...input$IN...})
  
  # reactive expression in render* (or another reactive expression)
  # output$OUT <- render*({...x()...})
  
})